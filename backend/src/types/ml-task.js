/**
 * @readonly
 * @enum {string}
 */
export const ML_TASKS = Object.freeze({
    FEED_CATEGORIZATION: 'classification.feed.item.category'
    //A classification tasks that determines the category of a feed item
});